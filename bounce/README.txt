**Bounce Benchmark**
Multithreaded applications measuring performance of Linux Network Stack for TCP connections

**TCP_SOCKET_BOUNCE**
Uses TCP socket for a single connection. Client creates as much traffic as possible. Bounce Server sends back packets with same content.
-Counts number of packets received on supplied IPv4/IPv6 address and Port
